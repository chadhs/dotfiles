# dotfiles: ghostty and other session apps launch $SHELL. systemd's user
# manager can outlive a logout/login with a stale pre-chsh value, leaving
# new sessions disagreeing with /etc/passwd. Mirror the chsh'd login shell
# so every session agrees (after deploy.sh chsh's to /usr/bin/zsh).
SHELL=/usr/bin/zsh
